export interface IAlquiler {
    agencia_ID: String,
    usuario_ID: String,
    vehiculo_ID: String,
    diasAlquiler: Number,
    inicioAlquiler: Date,
    finAlquiler: Date,
    pagoAbono: Number,
    garantia: String,
    estado: Boolean,
}
